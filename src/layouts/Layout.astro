---
import '../styles/global.css';
---
<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta name="generator" content={Astro.generator} />

		<title>IGSAEG</title>
	</head>

	<body>
        <div class="relative">
            <div class="absolute top-0 left-0 -z-1 grid md:grid-cols-6 grid-cols-3 w-full h-full px-margin">
                <span class="border-faint border-l opacity-25 max-md:hidden"></span>
                <span class="border-faint border-l opacity-25 max-md:hidden"></span>
                <span class="border-faint border-l opacity-25 max-md:hidden"></span>
                <span class="border-faint border-l opacity-25"></span>
                <span class="border-faint border-l opacity-25"></span>
                <span class="border-faint border-x opacity-25"></span>
            </div>
            
            <slot />
        </div>

		<script type="module"> import '../scripts/lenis.js'; </script>
	</body>
</html>