---
interface Props {
    title: string;
    items: string[];
    dark?: boolean;
    class?: string;
}
const { title, items, dark = false, class: className } = Astro.props;
const color = dark ? "--off-white" : "--black";
---
<div>
    <p class="eyebrow mb-2">{title}</p>
    <ul class="flex flex-col gap-1">
        {items.map((item) => <li><p class={`text-(${color}) ${className}`}>{item}</p></li>)}
    </ul>
</div>